title: 'Schemaless concurrency improvements: - Fixed an NPE when reloading a managed
  schema with no dynamic copy fields - Moved parsing and schema fields addition to
  after the distributed phase - AddSchemaFieldsUpdateProcessor now uses a fixed schema
  rather than always retrieving the latest, and holds the schema update lock through
  the entire schema swap-out process'
type: fixed
authors:
- name: Gregory Chanan
links:
- name: SOLR-6137
  url: https://issues.apache.org/jira/browse/SOLR-6137
