title: TermVectorComponent now supports per field overrides. Also, it now throws an
  error if passed in fields do not exist and warnings if fields that do not have term
  vector options (termVectors, offsets, positions) that align with the schema declaration.
  It also will now return warnings about
type: added
authors:
- name: gsingers
links:
- name: SOLR-1556
  url: https://issues.apache.org/jira/browse/SOLR-1556
