title: Allow Authentication Plugins to intercept internode requests on a per-request
  basis. The BasicAuth plugin now supports a new parameter 'forwardCredentials', and
  when set to 'true', user's BasicAuth credentials will be used instead of PKI for
  client initiated internode requests.
type: added
authors:
- name: janhoy
- name: noble
links:
- name: SOLR-12799
  url: https://issues.apache.org/jira/browse/SOLR-12799
