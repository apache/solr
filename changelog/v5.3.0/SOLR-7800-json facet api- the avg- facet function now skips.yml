title: 'JSON Facet API: the avg() facet function now skips missing values rather than
  treating them as a 0 value. The def() function can be used to treat missing values
  as 0 if that is desired. Example: facet:{ mean:"avg(def(myfield,0))" }'
type: other
authors: []
links:
- name: SOLR-7800
  url: https://issues.apache.org/jira/browse/SOLR-7800
